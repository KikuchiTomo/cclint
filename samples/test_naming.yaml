version: "1.0"

# Test configuration for naming_convention.lua rule
lua_scripts:
  - path: ../scripts/rules/builtin/naming_convention.lua
    enabled: true
    severity: warning

# No other rules
rules: []

# Include only main.cpp
include_patterns:
  - "*.cpp"

exclude_patterns: []

output_format: "text"
max_errors: 0
show_compiler_output: false

# Analysis settings
num_threads: 1
enable_cache: false
